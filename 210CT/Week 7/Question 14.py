def DFS (g,v):
    stack = new.stack
    visited = []
    stack.push(v)
    while len(stack) is not == 0:
        u = stack.pop
        if u not in visited:
            visited.append(u)
        for
def BFS(h,v):
    Q = new.queue
    visisted = []
    Q.enqueue(v)
    while len(Q)n != 0:
        u = Q.dequeue
        if u not in visited:
            visited.append(u)
    
